defmodule MiwayCreditCore.Organisations do
  @moduledoc """
  Reserved context boundary. Will own organisations, branches,
  memberships, and tenant-scoped access — the boundary that lets
  Miway CreditCore host more than one lending operation.

  No schema, migration, or function exists yet. Implementation is
  Step 5. See `docs/architecture/context_boundaries.md` for
  ownership, dependencies, and what this context must never do.
  """
end
